{
  "name": "danielconsultant-frontend",
  "version": "1.0.0",
  "description": "Combined deployment for Landing Page + DeskFlow",
  "scripts": {
    "build:landing": "cd dungeon-ai-landing && npm install && npm run build",
    "build:deskflow": "cd deskflow && npm install && npm run build",
    "build:all": "npm run build:landing && npm run build:deskflow && npm run copy:dist",
    "copy:dist": "node scripts/copy-dist.js",
    "clean": "rm -rf dist"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "private": true
}
